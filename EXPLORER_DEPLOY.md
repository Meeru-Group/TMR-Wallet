# TMR Explorer deployment

1. Keep Vercel Framework Preset = Express.
2. Root Directory = `./`.
3. Do not set an Output Directory.
4. Push the whole repository, including `public/`, `server.js`, `package.json`, and `vercel.json`.
5. Redeploy.

`/` is the Explorer UI. `/api/blocks` remains the JSON API.
